# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

**trackball-math** is a Node.js project in its initial setup phase. Based on the name, this appears to be a mathematical library or utility for trackball-related calculations.

## Current State

The repository is in early development with minimal structure:
- No `package.json` file yet (only `package-lock.json` exists)
- No source code implemented
- No build or test infrastructure configured

## Development Setup

Since the project structure is not yet established, future development will need to:

1. Create a `package.json` file to define project dependencies and scripts
2. Establish the source code structure (likely in `src/` or `lib/` directory)
3. Configure build tools and testing framework
4. Set up linting and formatting tools

## Notes for Future Development

- The project name suggests mathematical operations related to trackball input/rotation
- Consider using TypeScript for better type safety with mathematical calculations
- Will need to determine if this is a browser library, Node.js library, or both
- Mathematical libraries often benefit from unit tests with precise assertions

*This file will be updated as the project structure and tooling are established.*